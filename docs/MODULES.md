Areas → “If same Area name exists but soft-deleted, it is restored instead of creating duplicate.”

Properties → join condition with property_size_units only if not null.

Vendors →  company/vendor uniqueness rules.